<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= `${action} Cliente` %></title>
</head>
<body>
<%
    if (action === "actualizar") { %>
        <h1>Editando Cliente</h1>
    <% } else if (action === "ver") { %>
        <h1>Detalles de Cliente</h1>
    <% } else if (action === "eliminar") { %>
        <h1>Eliminar Cliente</h1>
    <% }
%>


<% if (action === "actualizar") { %>
    <form action="/clienteDetalle/actualizar/<%= cliente.codigo %>" method="POST">
        <label>Nombre:</label>
        <input type="text" name="nombre" value="<%= cliente.nombre %>" required><br>
        <label>Apellido:</label>
        <input type="text" name="apellido" value="<%= cliente.apellido %>" required><br>
        <label>Edad:</label>
        <input type="number" name="edad" value="<%= cliente.edad %>" required><br>
        <label>Teléfono:</label>
        <input type="text" name="telefono" value="<%= cliente.telefono %>" required><br>
        <label>Ciudad:</label>
        <input type="text" name="ciudad" value="<%= cliente.ciudad %>" required><br>
        <button type="submit">Actualizar</button>
    </form>
<% } else { %>
    <p>Nombre: <%= cliente.nombre %></p>
    <p>Apellido: <%= cliente.apellido %></p>
    <p>Edad: <%= cliente.edad %></p>
    <p>Teléfono: <%= cliente.telefono %></p>
    <p>Ciudad: <%= cliente.ciudad %></p>
        <% if (action === "eliminar") { %>
            <form action="/clienteDetalle/eliminar/<%= cliente.codigo %>" method="POST">
                <button type="submit">Eliminar</button>
            </form>
        <% } %>
<% } %>

</body>
</html>
